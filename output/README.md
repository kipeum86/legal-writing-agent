# Private Output Stub

Live generated outputs should be stored under `$LEGAL_AGENT_PRIVATE_DIR/output/` when `LEGAL_AGENT_PRIVATE_DIR` is set.

If the environment variable is unset, the agent falls back to `<repo>/output/` for local/dev compatibility only. Do not commit real client work product here.
