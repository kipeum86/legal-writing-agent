# Private Input Stub

Live revision inputs should be stored under `$LEGAL_AGENT_PRIVATE_DIR/input/` when `LEGAL_AGENT_PRIVATE_DIR` is set.

If the environment variable is unset, the agent falls back to `<repo>/input/` for local/dev compatibility only. Do not commit real client work product here.
